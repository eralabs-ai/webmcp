{
  "name": "webmcp-repo",
  "version": "0.0.0",
  "private": true,
  "description": "Repo tooling for the webmcp plugin: commit hooks and delegated checks. The publishable package lives in packages/webmcp-bridge.",
  "scripts": {
    "check": "node scripts/check.mjs",
    "lint": "npm --prefix packages/webmcp-bridge run lint && npm --prefix packages/webmcp-verify run lint",
    "typecheck": "npm --prefix packages/webmcp-bridge run typecheck && npm --prefix packages/webmcp-verify run typecheck",
    "test": "npm --prefix packages/webmcp-bridge test && npm --prefix packages/webmcp-verify test",
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.2.2",
    "@commitlint/config-conventional": "^21.2.2",
    "husky": "^9.1.7"
  }
}
